namespace Fashionista.Application.ProductAttributes.Commands.Validation
{
    internal static class Constants
    {
        internal const int NameMinLength = 2;
        internal const int NameMaxLength = 20;

        internal const string NameLengthMsg = "Color length must be between {0} and {1} characters long!";
    }
}
